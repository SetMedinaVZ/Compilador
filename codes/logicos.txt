program main {
    bool a, b, result;
    
    a = true;
    b = false;
    
    result = (a && b);  // AND lógico
    writeln(result);
    
    result = (a || b);  // OR lógico
    writeln(result);
    
    result = (!a);      // NOT lógico
    writeln(result);
}
